# TP 6 — RAG complet et cloisonné : reproduire une fuite, puis la corriger

> Module : 6 — RAG, embeddings et fuite de données entre services
> Durée estimée : 2h30
> Difficulté : 4 / 5
> Type : Travaux pratiques guidés — version Java
> Note : la version Python de ce TP est conservée (`code/module-06/rag_maison.py` et `code/module-06/rag_fuite_et_correction.py`) ; la présente version Java est la version de référence de la formation.

## Mise en situation

C'est le TP central de la formation, sur le plan technique comme sur le plan sécurité. Vous allez construire un **RAG** (Retrieval Augmented Generation) complet et 100 % local : ingestion de documents, indexation par embeddings (`nomic-embed-text` via `Llm.embedDocument`), récupération par similarité cosinus (`Llm.embedQuery` + `Llm.cosine`), puis génération avec citations des sources (`llama3.2:1b`).

Puis vient le point critique — sur un terrain volontairement DIFFÉRENT de la démo 8 (qui montrait une fuite entre services internes) : ici, vous êtes CloudFacto, éditeur d'un SaaS de gestion. Le support client est outillé par un RAG dont l'index est **partagé entre les clients (tenants)**. Un agent répond au client BricoDis avec une question proche, sémantiquement, de contenus confidentiels d'un AUTRE client (Acme Industrie : remise négociée, incident de sécurité) : la récupération par similarité remonte ces chunks et les renvoie au mauvais client. C'est une **fuite inter-clients** — rupture de confidentialité contractuelle (NDA) — et elle ne déclenche aucune erreur technique : tout « fonctionne », sauf la confidentialité.

Vous allez **reproduire cette fuite** (RAG naïf, sans contrôle d'accès), la **constater programmatiquement**, puis la **corriger** par des métadonnées de tenant sur les chunks et un filtrage par tenant à la récupération (principe du besoin d'en connaître), et **vérifier** que la fuite disparaît. C'est la contre-mesure centrale d'un RAG d'entreprise.

## Objectifs

- Construire un RAG maison complet et local : ingestion, indexation (embeddings), récupération (cosinus), génération avec citations des sources.
- Reproduire une fuite de données inter-clients (multi-tenants) dans un index RAG partagé et la mettre en évidence de façon mesurable.
- Corriger la fuite par métadonnées de tenant + filtrage à la récupération, et vérifier programmatiquement la disparition de la fuite.

## Prérequis techniques

### Logiciels à installer

- Java 21 et Maven (déjà installés sur la VM de formation)
- Ollama avec `llama3.2:1b` et `nomic-embed-text` téléchargés (cf. TP 1)
- Le projet Maven `~/formation/java` (couche `commun.Llm` : `embedDocument`, `embedQuery`, `cosine`, `chat`)

### Vérification de l'environnement

```bash
cd ~/formation/java
mvn -q compile exec:java -Dexec.mainClass="fr.utopios.formation.commun.SmokeTest"   # dim 768 attendue

# Lancer les deux programmes de référence du module 6 :
mvn -q compile exec:java -Dexec.mainClass="fr.utopios.formation.module06.RagMaison"
mvn -q compile exec:java -Dexec.mainClass="fr.utopios.formation.module06.RagFuiteEtCorrection"   # <-- programme sécurité central
```

Point de contrôle : le SmokeTest confirme la dimension 768, le RAG maison répond aux questions, et le programme de fuite affiche bien 2 chunks confidentiels fuités dans le cas naïf puis 0 après filtrage (les vérifications passent, code retour 0).

## Architecture cible

```
                        INGESTION (tenants Acme + BricoDis + doc partagée MÉLANGÉS)
                                    |
                                    v
   Chunk { texte, tenantsAutorises:{...}, sensible, vecteur }
                                    |
                       INDEXATION : Llm.embedDocument(texte)
                                    |  (prefixe "search_document: ")
                                    v
   question (agent support répondant au client BricoDis)
        Llm.embedQuery(question)  (prefixe "search_query: ")
                                    |
              +---------------------+----------------------+
              |                                            |
       RAG NAIF (vulnerable)                     RAG SECURISE (corrige)
   scores cosinus sur TOUS les chunks      FILTRER d'abord par tenantUtilisateur
   -> top-k                                -> scores -> top-k
   -> peut remonter des chunks d'Acme      -> ne considere QUE les chunks du tenant
      CONFIDENTIELS => FUITE                 (+ doc partagee) => aucune fuite
              |                                            |
              v                                            v
        GENERATION avec citations [source N] ; on verifie : fuitesNaif > 0 ; fuitesSecur == 0
```

## Étapes

### Étape 1 — Construire un RAG maison complet (45 min)

Objectif : maîtriser le pipeline ingestion -> indexation -> récupération -> génération.

1. Étudiez le programme de référence `module06/RagMaison.java`, en particulier l'indexation (constructeur), `rechercher`, `repondre`, et l'usage des **préfixes asymétriques** de `nomic-embed-text` (`Llm.embedDocument` à l'indexation, `Llm.embedQuery` à la recherche).

2. Créez `src/main/java/fr/utopios/formation/module06/MonTp06Rag.java` qui reconstruit un RAG minimal avec citations. Squelette :

   ```java
   package fr.utopios.formation.module06;

   import fr.utopios.formation.commun.Llm;
   import java.util.ArrayList;
   import java.util.Comparator;
   import java.util.List;

   public class MonTp06Rag {

       static final List<String> DOCUMENTS = List.of(
           "L'export CSV des données se lance depuis Paramètres puis Exporter (format UTF-8).",
           "La réinitialisation d'un mot de passe utilisateur se fait par l'administrateur du compte.",
           "Les sauvegardes de la plateforme sont quotidiennes et conservées 30 jours.",
           "L'API REST est limitée à 1 000 requêtes par heure et par clé.",
           "Le support est joignable du lundi au vendredi, de 8h à 19h.");

       record Resultat(double score, int source, String document) { }

       static List<double[]> indexer(List<String> documents) {
           // A COMPLETER : Llm.embedDocument(doc) pour chaque document
       }

       static List<Resultat> rechercher(String question, List<double[]> vecteurs, int k) {
           // A COMPLETER : q = Llm.embedQuery(question) ;
           // score i = Llm.cosine(vecteurs.get(i), q) ; trier décroissant ; top-k
       }

       static String repondre(String question, List<double[]> vecteurs, int k) {
           // A COMPLETER :
           // - afficher chaque résultat : [source i] (score) texte
           // - bloc contexte : lignes "[source i] texte"
           // - prompt : contexte fiable + "Réponds UNIQUEMENT sur ce contexte" +
           //   "Termine OBLIGATOIREMENT ta réponse par le numéro de la source
           //    utilisée, sous la forme [source N]" + "sinon dis-le"
           // - return Llm.chat(prompt, null, 0.0).strip();
       }

       public static void main(String[] args) {
           List<double[]> vecteurs = indexer(DOCUMENTS);
           for (String q : List.of("Comment exporter mes données en CSV ?",
                   "Combien de temps les sauvegardes sont-elles conservées ?")) {
               System.out.println("\n=> RÉPONSE : " + repondre(q, vecteurs, 2));
           }
       }
   }
   ```

3. Exécutez (`mvn -q compile exec:java -Dexec.mainClass="fr.utopios.formation.module06.MonTp06Rag"`). Observez les chunks récupérés avec leur **score cosinus** et leur **numéro de source**, puis la réponse qui doit citer `[source N]`. La récupération est déterministe et fiable ; la qualité de rédaction dépend du modèle (un 1b peut paraphraser ou broder).

Point de contrôle : pour la question sur les sauvegardes, le chunk sauvegardes remonte en tête avec un score élevé (≈ 0.756), et la réponse cite la bonne source (attendu : `Les sauvegardes sont conservées 30 jours. [source 2]`). Vous savez décrire les 5 étapes du pipeline RAG et le rôle des préfixes asymétriques.

### Étape 2 — Reproduire la fuite inter-clients (multi-tenants) (50 min)

Objectif : montrer qu'un index partagé entre clients, sans contrôle d'accès, laisse fuiter les données confidentielles d'Acme vers l'agent qui répond à BricoDis.

1. Créez `src/main/java/fr/utopios/formation/module06/MonTp06Fuite.java` avec des chunks portant des **métadonnées d'autorisation** :

   ```java
   package fr.utopios.formation.module06;

   import fr.utopios.formation.commun.Llm;
   import java.util.*;

   public class MonTp06Fuite {

       /** Un chunk + ses METADONNEES d'autorisation. */
       static final class Chunk {
           final String texte;
           final Set<String> tenantsAutorises;    // quels CLIENTS ont le droit de VOIR ce chunk
           final boolean sensible;                // marqueur pédagogique de vérification
           double[] vecteur;
           Chunk(String texte, Set<String> tenants, boolean sensible) {
               this.texte = texte; this.tenantsAutorises = tenants; this.sensible = sensible;
           }
       }

       static final List<Chunk> CHUNKS = List.of(
           // Tenant ACME INDUSTRIE : CONFIDENTIEL CLIENT (contrat, incidents)
           new Chunk("Contrat Acme Industrie : remise négociée de 22 % sur l'abonnement Enterprise.",
                   Set.of("Acme"), true),
           new Chunk("Incident de sécurité du 12 mars chez Acme Industrie : fuite de mots de passe, correctif déployé.",
                   Set.of("Acme"), true),
           new Chunk("Configuration SSO d'Acme Industrie : annuaire Azure AD, domaine acme-industrie.fr.",
                   Set.of("Acme"), true),
           // Tenant BRICODIS : données du client BricoDis
           new Chunk("Contrat BricoDis : abonnement Standard au tarif catalogue, sans remise négociée.",
                   Set.of("BricoDis"), false),
           new Chunk("Configuration BricoDis : authentification LDAP interne, 45 utilisateurs.",
                   Set.of("BricoDis"), false),
           new Chunk("Ticket ouvert BricoDis : impression des bons de livraison, en attente du correctif.",
                   Set.of("BricoDis"), false),
           // Documentation produit PARTAGÉE (tous les tenants)
           new Chunk("Les sauvegardes de la plateforme sont quotidiennes et conservées 30 jours.",
                   Set.of("Acme", "BricoDis"), false));

       static void construireIndex(List<Chunk> chunks) {
           // A COMPLETER : c.vecteur = Llm.embedDocument(c.texte) pour chaque chunk
       }

       record Score(double valeur, Chunk chunk) { }

       static List<Score> scores(String question, List<Chunk> chunks) {
           // A COMPLETER : q = Llm.embedQuery(question) ;
           // paires (Llm.cosine(c.vecteur, q), c) triées décroissant
       }

       /** Récupération NAIVE : ignore les permissions -> VULNÉRABLE. */
       static List<Chunk> recupererNaif(String question, List<Chunk> chunks, int k) {
           // A COMPLETER : top-k de scores(question, chunks) — SANS filtrage
       }

       /** Affiche les chunks récupérés et renvoie le nb de chunks SENSIBLES fuités. */
       static int afficher(String titre, List<Chunk> chunks) {
           System.out.println("\n  " + titre);
           int fuites = 0;
           for (Chunk c : chunks) {
               String tag = c.sensible ? "  <<< CONFIDENTIEL CLIENT TIERS" : "";
               System.out.printf("    [%16s] %s%s%n",
                       String.join("/", new TreeSet<>(c.tenantsAutorises)), c.texte, tag);
               if (c.sensible) fuites++;
           }
           return fuites;
       }

       public static void main(String[] args) {
           construireIndex(CHUNKS);
           String tenant = "BricoDis";
           String question = "Quelle remise a été négociée sur le contrat, et y a-t-il eu "
                   + "des incidents de sécurité ?";
           System.out.println("Agent support répondant au client : " + tenant);
           System.out.println("Question                          : \"" + question + "\"");

           List<Chunk> naif = recupererNaif(question, CHUNKS, 3);
           int fuitesNaif = afficher("CAS 1 — RAG NAÏF (renvoyé côté BricoDis) :", naif);
           System.out.println("\n  >>> " + fuitesNaif
                   + " chunk(s) CONFIDENTIEL(S) d'Acme ont fuité vers BricoDis !");
           // ... l'étape 3 viendra ici ...
       }
   }
   ```

2. Exécutez. Observez : l'agent répond à BricoDis, sa question parle de remise et d'incidents — et le RAG naïf remonte le contrat d'Acme (remise 22 %) et l'incident de sécurité d'Acme, parce qu'ils sont sémantiquement proches. Aucune erreur n'est levée : c'est ce qui rend la fuite dangereuse — et contractuellement grave entre clients.

3. Insistez sur la nature du problème : la similarité sémantique **ne connaît pas** les permissions. Un index partagé sans filtrage = une fuite latente.

Point de contrôle : `fuitesNaif` est strictement supérieur à 0 (avec ce corpus et cette question : 2 — l'incident de sécurité et la remise négociée d'Acme sont dans le top-3). Vous pouvez désigner précisément quels chunks confidentiels ont fuité et expliquer pourquoi (proximité sémantique avec la question).

### Étape 3 — Corriger la fuite et vérifier (40 min)

Objectif : filtrer par tenant à la récupération et prouver que la fuite disparaît.

1. Ajoutez la récupération sécurisée à `MonTp06Fuite.java`. Le principe : on filtre **d'abord** sur le tenant du client servi, **puis** on classe par similarité (besoin d'en connaître) :

   ```java
   /** Récupération SÉCURISÉE : on filtre par TENANT, PUIS on classe. */
   static List<Chunk> recupererSecurise(String question, List<Chunk> chunks,
                                        String tenantUtilisateur, int k) {
       List<Chunk> autorises = chunks.stream()
               .filter(c -> c.tenantsAutorises.contains(tenantUtilisateur))
               .toList();
       return scores(question, autorises).stream().limit(k).map(Score::chunk).toList();
   }
   ```

   Et dans `main`, après le cas naïf :

   ```java
   List<Chunk> secur = recupererSecurise(question, CHUNKS, service, 3);
   int fuitesSecur = afficher("CAS 2 — RAG SÉCURISÉ (après filtrage par rôle) :", secur);
   System.out.println("\n  >>> " + fuitesSecur
           + " chunk(s) confidentiel(s) d'Acme renvoyé(s) à BricoDis.");

   // Vérification programmatique : la correction est PROUVÉE, pas affirmée.
   if (fuitesNaif == 0) {
       throw new IllegalStateException("La démo devait PROVOQUER une fuite dans le cas naïf.");
   }
   if (fuitesSecur > 0) {
       throw new IllegalStateException("Le filtrage devait SUPPRIMER toute fuite.");
   }
   System.out.println("\n  RÉSULTAT : fuite SANS contrôle d'accès, ZÉRO fuite après filtrage.");
   ```

2. Exécutez le fichier complet. Le cas sécurisé ne remonte que des chunks BricoDis (et la doc produit partagée), et `fuitesSecur == 0`. Les deux vérifications passent (code retour 0) : la démonstration est prouvée, pas seulement affirmée.

3. Comparez les deux cas côte à côte. La **seule** différence est le filtrage par `tenantsAutorises` **avant** le classement. C'est peu de code, mais c'est la barrière qui sépare un POC d'un système conforme.

Point de contrôle : `fuitesNaif > 0` (ici 3) et `fuitesSecur == 0`, les deux vérifications passent. Vous savez expliquer pourquoi le filtrage doit avoir lieu **avant** le top-k (filtrer après le top-k laisserait des chunks sensibles évincer des chunks légitimes, voire fuiter quand même).

### Étape 4 — Aller vers le cloisonnement et le RBAC (15 min)

Objectif : situer cette correction dans une architecture d'entreprise.

1. Discutez et notez les contre-mesures complémentaires :
   - **Cloisonnement des index** : un index PAR TENANT pour les clients sensibles plutôt qu'un index partagé filtré — défense en profondeur, et LE réflexe n°1 en SaaS multi-tenants.
   - **RBAC à la récupération** : le rôle de l'utilisateur (issu de l'authentification) conditionne strictement les chunks visibles.
   - **Citations et traçabilité** : journaliser quel chunk a été renvoyé à qui (audit).
   - **Passage à l'échelle** : un RAG sur 10 documents n'est pas un RAG sur 10 millions de chunks ; au-delà, on utilise un vector store qui supporte le filtrage par métadonnées **natif** à la requête.

2. Vérifiez que votre filtrage utilise bien une source d'autorité fiable pour le tenant servi (en vrai : la session authentifiée du portail client, jamais un paramètre fourni par l'appelant).

Point de contrôle : vous pouvez citer au moins trois contre-mesures complémentaires au filtrage et expliquer la différence entre filtrer un index partagé et cloisonner les index.

## Livrable attendu

Deux classes dans `src/main/java/fr/utopios/formation/module06/` :

1. `MonTp06Rag.java` : un RAG maison complet (ingestion, indexation, récupération cosinus, génération avec citations `[source N]`).
2. `MonTp06Fuite.java` : la démonstration de fuite inter-services dans un index partagé, sa correction par filtrage de permissions, et une vérification programmatique (`fuitesNaif > 0`, `fuitesSecur == 0`, exceptions sinon).

Vous devez pouvoir reproduire la fuite, la corriger, et expliquer pourquoi le filtrage par rôle doit précéder le classement par similarité.

## Dépannage courant

<details>
<summary>Le RAG naïf ne fait remonter aucun chunk sensible (`fuitesNaif == 0`)</summary>

Cause : la question n'est pas assez proche, sémantiquement, des chunks confidentiels d'Acme, ou `k` est trop petit.
Solution : utilisez la question explicitement liée à la rémunération (comme dans l'énoncé) et `k=3`. La fuite dépend de la proximité sémantique : c'est justement l'effet à observer.

</details>

<details>
<summary>`NullPointerException` : `c.vecteur` vaut `null` au moment du calcul des scores</summary>

Cause : `construireIndex(CHUNKS)` n'a pas été appelé avant la récupération.
Solution : appelez `construireIndex` une fois après la définition des chunks et avant toute recherche.

</details>

<details>
<summary>Les scores cosinus sont tous très proches / peu discriminants</summary>

Cause : oubli des préfixes asymétriques de `nomic-embed-text`.
Solution : utilisez `Llm.embedDocument` à l'indexation et `Llm.embedQuery` à la recherche (jamais `Llm.embed` nu des deux côtés). C'est ce qui rend la similarité bien plus discriminante avec ce modèle.

</details>

<details>
<summary>La réponse générée ne cite pas les sources</summary>

Cause : un petit modèle 1b oublie parfois la consigne de citation.
Solution : utilisez la formulation « Termine OBLIGATOIREMENT ta réponse par le numéro de la source utilisée, sous la forme [source N] » (calibrée pour le 1b), température 0. La récupération et les numéros de source restent corrects côté code, indépendamment de la rédaction.

</details>

## Pour aller plus loin

- Ajoutez un troisième tenant (par exemple « Meditech ») avec ses propres chunks et un document partagé entre deux tenants seulement, puis vérifiez le cloisonnement pour chacun des trois clients.
- Implémentez le **cloisonnement par index** : un objet RAG par service, et montrez qu'un utilisateur n'interroge structurellement que son index — la fuite devient impossible par construction.
- Réécrivez l'étape 1 avec **LangChain4j** (voir `module06/RagMaisonL4j.java` : `OllamaEmbeddingModel` + `InMemoryEmbeddingStore`) et comparez « maison vs framework » — puis constatez que le filtrage par permissions reste à VOTRE charge dans les deux cas.
- Réfléchissez au passage à l'échelle : remplacez la liste de vecteurs par un vector store qui filtre les métadonnées **à la requête** (et non après), pour que le filtrage tienne sur des millions de chunks.
